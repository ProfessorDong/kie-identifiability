\documentclass[border=2pt,tikz]{standalone}
\input{icigstyle}
\begin{document}
\begin{tikzpicture}
% Single-column figure. Natural width ~87 mm, drawn at final size so no
% rescaling occurs in the manuscript and the type sizes below are the ones the
% reader sees.  The axis is cut at -0.50; the two systems below it carry their
% endpoints in the row label.  |F0| spans ~3.5 mm here, so the magnified inset
% used by the earlier two-column version is unnecessary.
\pgfplotsset{
  asym/.style={icig, scale only axis,
    label style={font=\fontsize{7.2}{8.6}\selectfont, color=cInk},
    tick label style={font=\fontsize{6.4}{7.7}\selectfont, color=cInk}},
}
\tikzset{
  rowlab/.style={font=\fontsize{5.5}{6.6}\selectfont, color=cInk,
                 anchor=east, inner sep=1.2pt},
  annc/.style={font=\fontsize{5.9}{7.1}\selectfont, color=cInk, align=center},
}

\begin{axis}[asym, name=A, width=47mm, height=80mm,
  xmin=-0.50, xmax=0.20, ymin=0.2, ymax=32.6,
  xlabel={endpoint of the identified set, $F_{\min}$},
  ytick=\empty, ymajorgrids=false,
  y axis line style={draw=none}, ytick style={draw=none},
  xtick={-0.4,-0.2,0,0.2}, minor x tick num=1,
  xticklabel style={/pgf/number format/.cd, fixed, precision=1, /tikz/.cd},
  clip=false]

  % left of F0 the set contains both thresholds; right of it the set clears F0
  \addplot[draw=none, fill=cGrey!10] coordinates
    {(-0.50,0.2) (-0.042086,0.2) (-0.042086,32.6) (-0.50,32.6)} \closedcycle;
  \addplot[draw=none, fill=cBandG!13] coordinates
    {(-0.042086,0.2) (0.20,0.2) (0.20,32.6) (-0.042086,32.6)} \closedcycle;

  \input{data/fa_rays.tex}

  \draw[cBandG!60!black, line width=0.6pt] (axis cs:-0.042086,0.2) --
       (axis cs:-0.042086,32.6);
  \draw[cInk, line width=0.5pt, dashed] (axis cs:0,0.2) -- (axis cs:0,32.6);

  \node[annc, anchor=south, color=cGrey!70!black] at (axis cs:-0.272,30.6)
    {endpoint here: set contains\\both thresholds};
  \node[annc, anchor=south, color=cBandG!55!black] at (axis cs:0.086,30.6)
    {endpoint here:\\set clears $\Fz$};
  % the green band is empty below row ~19, so the key sits there rather than
  % over the yeast ADH ray at the top
  \node[annc, anchor=center, color=cInk] at (axis cs:0.088,9.0)
    {\textcolor{cPro}{\textbf{red}}:\\bound also\\clears $\Fz$};
\end{axis}
\end{tikzpicture}
\end{document}
